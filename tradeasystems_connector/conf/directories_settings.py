import os

# Framework
project_path = os.path.dirname(os.path.dirname(os.path.realpath(__file__)))

# Must be user settings
# database_path = project_path + os.sep + 'database' + os.sep
# log_path = project_path + os.sep + 'logs' + os.sep

# Algorithm : each algo or execution will have this outputs

# output_result_path = 'output' + os.sep
# pic_path = 'output' + os.sep + 'pic' + os.sep

#
# pathCreate = [log_path, output_result_path, pic_path]
# for path in pathCreate:
#     if not os.path.isdir(path):
#         os.mkdir(path)
